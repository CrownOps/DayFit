services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: dayfit
      POSTGRES_PASSWORD: dayfit
      POSTGRES_DB: dayfit
    ports:
      - "5432:5432"
    volumes:
      - dayfit_db_data:/var/lib/postgresql/data

volumes:
  dayfit_db_data:
